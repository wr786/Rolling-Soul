```python
# 定义全局变量
def read_data()					# 读入各种数据
class Button					# 创建按钮类，避免重复开发，下类似
class Obstacle					# 创建障碍物类
class Bullet					# 创建子弹类
class Weapon					# 创建武器类
class Player					# 创建玩家类
class Knight(Player)			# 继承Player类生成Knight类，下类似
class Assassin(Player)
class Paladin(Player)
class Enemy						# 创建敌人类
def get_death()					# 判断角色死亡，并做出后续操作
def portal_create()				# 创建传送门，用于进入下一关卡
def slotmachine_create(x, y)	# 在指定地点生成老虎机，用于摇取武器
def slotmachine_play()			# 启动老虎机，摇奖，并提供奖励等
def setting_create()			# 弹出设置界面
def volume_control()			# 音量控制
def draw_map()					# 随机生成地图背景
def generate_map_cells()		# 生成背景图块
def next_level(flag = True)		# 进入下一关，flag关系到剧情选择
def choose_role(pos)			# 人物选择
def start_view()				# 开始界面
def generate_skillCD_png()		# 利用Pillow处理图片，使得技能条的回复看起来是动态的
def show_enemy_pos()			# 敌人出场前展现动画特效
def Beginning_all()				# 播放开头剧情
def next_plot(pos)				# 剧情推进，并判断玩家做出的选择
def show_plot()					# 剧情展示
def obstacle_map()				# 绘制地图中的障碍物
# 程序初始化、启动程序
```

